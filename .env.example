# Required when running the HTTP server. Generate a long, random value locally.
MCP_AUTH_TOKEN=replace-with-a-long-random-token

# "mock" is the only supported provider mode in the initial release.
DATA_PROVIDER=mock
